<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="GameSync.App.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:GameSync.App.ViewModels">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="History" />
            <Button Grid.Column="1" Content="Refresh" Command="{x:Bind ViewModel.RefreshCommand}" />
        </Grid>

        <InfoBar
            Grid.Row="1"
            IsOpen="{x:Bind ViewModel.IsInfoBarOpen, Mode=TwoWay}"
            Title="{x:Bind ViewModel.InfoBarTitle, Mode=OneWay}"
            Message="{x:Bind ViewModel.InfoBarMessage, Mode=OneWay}"
            Severity="{x:Bind ViewModel.InfoBarSeverity, Mode=OneWay}"
            IsClosable="True" />

        <Grid Grid.Row="2">
            <ProgressRing
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />

            <ListView
                ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedEntry, Mode=TwoWay}"
                AutomationProperties.Name="Save history">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:HistoryEntryItem">
                        <Grid Padding="0,8" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind TimestampText}" Style="{StaticResource CaptionTextBlockStyle}" />
                            <TextBlock Grid.Column="1" Text="{x:Bind GameDisplay}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2" Text="{x:Bind AuthorName}" TextTrimming="CharacterEllipsis" />
                            <StackPanel Grid.Column="3">
                                <TextBlock Text="{x:Bind Message}" TextWrapping="WrapWholeWords" />
                                <TextBlock Text="{x:Bind ShortSha}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button
                Style="{StaticResource AccentButtonStyle}"
                Content="Restore this version"
                Command="{x:Bind ViewModel.RestoreCommand}"
                AutomationProperties.Name="Restore selected version" />
            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Width="28" Height="28" />
            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="A local backup is created before restore." />
        </StackPanel>
    </Grid>
</Page>
