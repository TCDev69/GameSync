<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="GameSync.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="GameSync"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="GameSync" AutomationProperties.Name="GameSync title bar">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <StackPanel Grid.Row="1" Margin="12,0,12,8" Spacing="8">
            <InfoBar
                x:Name="UpdateBar"
                IsOpen="False"
                IsClosable="True"
                Severity="Informational"
                Title="Update available">
                <InfoBar.ActionButton>
                    <Button Content="Install update" Click="InstallUpdate_Click" />
                </InfoBar.ActionButton>
            </InfoBar>

            <Border
                x:Name="ActivityPanel"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="14,10"
                Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibility}}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <FontIcon
                        Glyph="&#xE895;"
                        FontSize="18"
                        VerticalAlignment="Top"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Grid.Column="1" Spacing="6">
                        <TextBlock
                            x:Name="ActivityTitleText"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{Binding Title}" />
                        <TextBlock
                            x:Name="ActivityMessageText"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{Binding Message}"
                            TextWrapping="WrapWholeWords"
                            Opacity="0.85" />
                        <ProgressBar
                            x:Name="ActivityProgressBar"
                            Maximum="100"
                            Value="{Binding Progress}"
                            IsIndeterminate="{Binding IsIndeterminate}"
                            ShowError="{Binding IsError}" />
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <NavigationView
            x:Name="RootNavigation"
            Grid.Row="2"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            PaneDisplayMode="Left"
            OpenPaneLength="220"
            IsPaneToggleButtonVisible="True"
            SelectionChanged="RootNavigation_SelectionChanged"
            AutomationProperties.Name="Main navigation">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Library" Tag="Library" AutomationProperties.Name="Library">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7FC;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="History" Tag="History" AutomationProperties.Name="History">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE81C;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </NavigationView>

        <Frame
            x:Name="OnboardingFrame"
            Grid.Row="2"
            Visibility="Collapsed" />
    </Grid>
</Window>
