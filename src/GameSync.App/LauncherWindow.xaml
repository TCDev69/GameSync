<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="GameSync.App.LauncherWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="GameSync Launcher">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="GameSync Launcher">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <StackPanel Grid.Row="1" Padding="24" Spacing="12" MaxWidth="480">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind ViewModel.GameTitle, Mode=OneWay}" />
            <TextBlock
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.ProcessIdText, Mode=OneWay}" />
            <ProgressBar
                Maximum="100"
                Value="{x:Bind ViewModel.ProgressPercent, Mode=OneWay}"
                IsIndeterminate="{x:Bind ViewModel.IsProgressIndeterminate, Mode=OneWay}"
                ShowError="{x:Bind ViewModel.IsError, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                HorizontalAlignment="Stretch"
                AutomationProperties.Name="Launch progress" />
            <InfoBar
                IsOpen="{x:Bind ViewModel.IsError, Mode=OneWay}"
                Severity="Error"
                Title="Launch problem"
                Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                IsClosable="False" />
            <Button
                Content="I've finished playing"
                Command="{x:Bind ViewModel.ConfirmSessionEndCommand}"
                Visibility="{x:Bind ViewModel.AwaitingSessionEnd, Mode=OneWay}"
                HorizontalAlignment="Left" />
            <Button Content="Close" Click="Close_Click" HorizontalAlignment="Left" />
        </StackPanel>
    </Grid>
</Window>
